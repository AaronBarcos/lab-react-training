
function BoxColor(props) {
    
  return (
    <div style={{width: "300px", height: "50px", backgroundColor: `rgb(${props.r}, ${props.g}, ${props.b})` }}>
        <div>
            
        </div>
    </div>
  );
}

export default BoxColor;